package slogo.view;

import javafx.stage.Stage;

public class View {

    private Stage stage;

    public View(Stage stage) {
        this.stage = stage;
    }

    public void run() {
        // page = new MenuPage(stage, this);
        // page.setUp();
        // stage.setScene(new Scene(page.getGroup(), width, height));
        // stage.setMaximized(true);
        // stage.show();
    }
}
